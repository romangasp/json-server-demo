const projects = require("./data/projects");
const openProjects = require("./data/openProjects");
const closeProjects = require("./data/closeProjects");
const projectsPage = require("./data/projectsPage");
const purchaseOrder = require("./data/purchaseOrder")

module.exports = {
  projects,
  openProjects,
  closeProjects,
  projectsPage,
  purchaseOrder
};
