const projects = require("./data/projects");
const openProjects = require("./data/openProjects");
const closeProjects = require("./data/closeProjects");
const pageProjects = require("./data/projects");

module.exports = {
  projects,
  openProjects,
  closeProjects,
  pageProjects,
};
